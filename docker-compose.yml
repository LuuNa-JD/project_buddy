version: '3.8'

services:
  discord-bot-buddy:
    build:
      context: .
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}
      - EMAIL_SENDER=${EMAIL_SENDER}
      - EMAIL_PASSWORD=${EMAIL_PASSWORD}
    restart: no
